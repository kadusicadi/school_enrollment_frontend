/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  swcMinify: true,
  output: 'standalone',
  // experimental: {
  //   appDir: true,
  // },
}

module.exports = nextConfig
